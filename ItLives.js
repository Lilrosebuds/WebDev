const myName = "Frankenstien's Monster"

let favColor = "What's a color"

favColor = "blue"

console.log(favColor)

let age = 0

Isitaperson = {
    name: "Frankenstein's Monster",
    age: 0,
    favColor: "What's a Color",
    favFoods: ["I", "Don't", "Know"]
}

favThings = ["french", "blind people"]

const isAlive = true

const isDead = true

function describePerson(person) {
    alert("MISSING MONSTER ALERT: " + person.name)
    alert(`MISSING MONSTER ALERT: ${person.name} missing since 1818, made of dead people's body parts`)
}

describePerson(Isitaperson)